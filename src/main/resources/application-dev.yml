# 开发环境
server:
  port: 8080

spring:
  datasource:
    name: lchapp
    url: jdbc:mysql://localhost:3306/lchapp?serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.samuel.springboot.entity

#showSql
logging:
  level:
    com:
      example:
        mapper : debug